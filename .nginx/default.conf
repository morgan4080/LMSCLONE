server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;
    root  /usr/share/nginx/html;
    include /etc/nginx/mime.types;

    location /sales-dashboard/ {
        try_files $uri /sales-dashboard/index.html;
    }
}
